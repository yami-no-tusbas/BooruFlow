# Legacy compatibility package

This directory contains thin import and command wrappers for historical CLI
entry points. The retired Tkinter interface remains available through Git
history, but is no longer shipped or tested. New code must import `booruflow`
directly.
