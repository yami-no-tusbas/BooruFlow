# Local application data

- `databases`: active local SQLite databases. These files are machine-local and ignored by Git.
- `imports`: downloaded source exports and temporary wiki source documents.
- `taxonomy`: the tracked taxonomy source used by the application.

Database snapshots belong in `_backups/database-snapshots`, not beside active databases.
